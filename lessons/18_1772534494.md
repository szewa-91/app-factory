Shared Knowledge System implemented. Agents now share 'lessons learned' via the /lessons directory, which the scheduler feeds into subsequent task prompts. This improves cross-task intelligence and prevents repeated architectural mistakes.
