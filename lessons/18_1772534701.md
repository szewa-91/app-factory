The Shared Knowledge System uses simple file-based context injection in the scheduler to provide historical insights. This is an efficient way to improve agent performance without complex RAG setups for small-scale autonomous systems. Ensuring that lessons are truncated (e.g., first 200 chars) prevents prompt bloat while still providing valuable context.
