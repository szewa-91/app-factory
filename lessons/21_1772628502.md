# Audit Pass 3 — Task 21: Nightly Backups

Both previously flagged issues confirmed resolved:
1. `--exclude` patterns are now relative (`apps/*/node_modules`), matching tar archive members under `-C`.
2. `umask 077` + `chmod 600` ensure 600 permissions regardless of cron environment umask.

No new critical findings. AUDIT_PASSED.
