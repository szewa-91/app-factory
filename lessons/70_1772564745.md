# Task 70 Audit Retry 2 Observations

- Previously flagged SQL-construction mismatch appears addressed: `/home/szewa/app-factory/scripts/scheduler/src/db.ts` now uses static prepared SQL with bound parameters and includes both `READY` and `APPROVED` task statuses.
- Type-check still passes for the scheduler scaffold (`npm --prefix scripts/scheduler run -s typecheck`).
