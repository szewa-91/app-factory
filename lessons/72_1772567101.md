# Lesson Learned: Keep Retry Logic Symmetric Across Developer and Auditor Paths

Scheduler parity is easiest to preserve when retry transitions are centralized and reused by both developer-failure and audit/verification-failure branches. If each branch updates status/retry_count independently, subtle drift appears quickly (different max-retry messages, missing context capture, or inconsistent git reset behavior).
