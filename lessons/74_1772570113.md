# Task 74 Audit Attempt 2 - Non-Critical Observation

- The previously flagged lock-contention observability gap is resolved: `scripts/scheduler/src/index.ts` now logs `[LOCK] Scheduler already running ... Exiting.` and runtime evidence appears in `factory.log` during the retry window.
- `scheduler.lock` appears in the Task 74 commit as a runtime artifact; this is non-critical for correctness/security but can add commit noise.
