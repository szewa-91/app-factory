# Audit attempt 2 observations

- Non-critical: `next build` reports multiple lockfiles and auto-selects workspace root (`/home/szewa`). Consider setting `turbopack.root` in `next.config.ts` or removing extra lockfile ambiguity to avoid accidental root mis-detection in CI.
