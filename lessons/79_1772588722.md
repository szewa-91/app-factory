# Audit 79 - Non-critical observations

- The previous feedback about missing tests was addressed: `__tests__/cv.test.tsx` and `__tests__/PrintButton.test.tsx` were added, and `vitest run` passes (8/8).
- A small quality gap remains: the test named "renders all experience entries" does not explicitly assert all entries from `data/cv.ts` (there is no direct assertion for `Software Development Academy`), and the skill-groups test validates only part of the categories.
